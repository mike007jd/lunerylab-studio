{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-runtime",
  "description": "Allow the bundled local Studio server to call Lunery Lab desktop runtime commands. The capability is scoped to the bootstrap WebView only — `remote.urls` is intentionally empty so no remote URL (including any 127.0.0.1 port other than the one Tauri chose for its bootstrap index.html) can invoke core IPC. Studio's own invoke calls go via the bootstrap origin.",
  "windows": ["main"],
  "remote": {
    "urls": []
  },
  "permissions": ["core:default"]
}
